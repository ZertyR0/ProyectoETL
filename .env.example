# Variables de entorno para deployment
# Copia este archivo a .env y configura tus valores

# Base de datos origen
DB_HOST_ORIGEN=localhost
DB_PORT_ORIGEN=3306
DB_USER_ORIGEN=root
DB_PASSWORD_ORIGEN=tu_password
DB_NAME_ORIGEN=gestionproyectos_hist

# Base de datos destino (DataWarehouse)
DB_HOST_DESTINO=localhost
DB_PORT_DESTINO=3306
DB_USER_DESTINO=root
DB_PASSWORD_DESTINO=tu_password
DB_NAME_DESTINO=dw_proyectos_hist

# Flask
FLASK_ENV=production
SECRET_KEY=cambia_esto_por_algo_seguro_y_aleatorio
PORT=5001

# Seguridad (opcional)
PM_TOKEN=tu_token_pm_aqui
ADMIN_TOKEN=tu_token_admin_aqui
