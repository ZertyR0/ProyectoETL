# Configuración para Nixpacks (Railway)
[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements-all.txt"]

[phases.build]
cmds = ["chmod +x start.sh"]

[start]
cmd = "bash start.sh"
